# Dependencies
node_modules/

# Build output
ui/dist/
server/dist/
build/

# Data (local, not committed)
server/data/
server/server/data/
*.db
*.db-shm
*.db-wal
*.db-journal

# Session logs (generated per-run)
logs/

# Environment (contains local paths)
.env

# Restart marker (transient, created by /api/restart)
.restart-requested

# Engine build artifacts
engine/acestep.cpp/
engine/build/
engine/deps/
engine/.cache/

# TRT verification probe artifacts (local only)
build-trtcheck/
build/
build-*/
.toolchain/

# Local Python venv (not committed)
.venv/

# Local model weights / bundles (not committed)
models/

# Downloaded dependency archives (local only)
engine/deps/*.zip
*.zip

# Binaries & compiled objects
*.exe
!Essentia/*.exe
*.dll
*.lib
*.obj
*.o
*.a
*.so
*.dylib
*.pdb
*.ilk
*.exp

# Audio files
*.wav
!noise_samples/*.wav
*.mp3
*.flac
*.ogg
*.aac
*.wma
*.m4a
*.bf16

# Model weights
*.gguf
*.safetensors
*.pt
*.pth
*.onnx

# Downloaded tools (whisper.cpp, etc.)
tools/*
!tools/onnx-export/
!tools/trt/
!tools/mcp-lyricstudio/
!tools/install_trt.ps1
!tools/setup_venv.ps1
!tools/install_trt.sh
!tools/setup_venv.sh
!tools/build-trt-engine.py
tools/mcp-lyricstudio/node_modules/

# Test output logs
engine/tests/*.log

# Python
__pycache__/
*.pyc
*.pyo

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Agents & dev tooling (local only)
.agents/
.agent/
.claude/
.claude-plans/
.gemini/

# Dev-only scripts (local only, not needed by end users)
dev.bat
dev-rebuild.bat
server/restart-loop.cmd

# Local-only content
benchmark-results/
docs/plans/
scratch/

# Release build artifacts (but keep scripts and configs)
release/staging/
release/out/
release/.node-cache/
release/node_modules/
release/package-lock.json
release-staging/
runtime/
*.tar.gz

# Windows NUL device phantom
nul

# i18n dev-only utility scripts
ui/add_strings.mjs
ui/temp_strings.json

# TensorRT-LLM (local dev only)
engine/trtllm-libs/
engine/trtllm-include/

# TRT experiment files (local dev only)
test_trtllm_*.cpp
test_trtllm_*.h
create_test_onnx.py
test_smoke.*
rebuild_engine.*
Important.md

# Docker (container-specific overrides, not committed)
docker-compose.override.yml
# Feasibility spike scratch (throwaway, local only)
.spike-w0-6/
# ORT-removal proof spike (throwaway, local only)
.spike-w1-1/
.spike-w1-1-build*.log
# Encoder TRT engine build artifacts (safetensors, ONNX, engines — local only)
.enc-build/

# Throwaway build/probe scratch at repo root (regenerable, local only)
*_w0*.cmd
build_w43.cmd
probe_props*
/*.log

# E2E gen proof artifacts (audio, drive scripts, logs, VRAM samples — local only)
w0-5-output/
w4-1-output/
w4-2-3-output/
setup_trt_venv.ps1
