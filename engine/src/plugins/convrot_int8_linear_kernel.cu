/*
 * convrot_int8_linear_kernel.cu — Empty stub.
 *
 * Phase 1 of the ConvRot INT8 migration removed all hand-written CUDA kernels
 * from this file. The full pipeline (ConvRot rotation + per-row INT8 quant +
 * INT8×INT8 GEMM + dequant+bias epilogue) now lives in a single AOT-compiled
 * Triton kernel:
 *
 *   tools/onnx-export/trt_plugins/convrot_int8_kernel.py
 *
 * The rotation is implemented as in-register H_4 Kronecker butterfly stages
 * (no H matrix in shared memory), which works uniformly for all valid group
 * sizes {4, 16, 64, 256, 1024} and preserves single-launch fusion.
 *
 * The .cubin is generated by tools/onnx-export/compile_triton_plugin.py and
 * embedded into engine/src/plugins/assets/convrot_int8_kernel_cubin.h. The
 * plugin loads it at runtime via the CUDA Driver API (cuModuleLoadData +
 * cuModuleGetFunction + cuLaunchKernel) — see convrot_int8_linear_plugin.cpp.
 *
 * The companion header convrot_int8_linear_kernel.cuh still declares the old
 * launch helpers for ABI compatibility with any downstream code that might
 * still reference them; nothing in the plugin calls them anymore. If you need
 * to reintroduce hand-written CUDA kernels (e.g. for a non-Triton fallback
 * path), do it here.
 */

#ifdef HOT_STEP_TRT
// Intentionally empty — all kernels migrated to Triton in Phase 1.
#endif
